import { CategoryErrorPipe } from './category-error.pipe';

describe('CategoryErrorPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryErrorPipe();
    expect(pipe).toBeTruthy();
  });
});
